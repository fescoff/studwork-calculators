rotating($duration = 1.5s, $timing-function = linear)
  animation rotating $duration $timing-function infinite

  @keyframes rotating
    from
      transform rotate(0deg)

    to
      transform rotate(360deg)
